const request = require("supertest");
const db = require("../data/dbConfig");
const server = require("../api/server");

describe("getAll()", () => {
  describe("returns all characters", () => {
    it.todo("do things")
  });
});
